import { React } from "react";
import Connect from "../../components/Connect";

const HomePage = () => {
    return (
        <>
            <Connect />
        </>
    );
};

export default HomePage;
